# Web framework
Django>=5.2,<6.1

# PostgreSQL driver (psycopg 3 - supports Python 3.14)
psycopg[binary]>=3.2

# Image processing for ImageField (avatar)
Pillow>=11.0

# Render form fields with Bootstrap classes neatly in templates
django-widget-tweaks>=1.5

# Read environment variables from .env file
python-dotenv>=1.0

# --- Dependencies for Production (Render/Cloud) ---
gunicorn>=21.2.0
whitenoise>=6.6.0
dj-database-url>=2.1.0
