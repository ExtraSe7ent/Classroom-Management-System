# Web framework
Django>=5.2,<6.1

# PostgreSQL driver (psycopg 3 - hỗ trợ Python 3.14)
psycopg[binary]>=3.2

# Xử lý ảnh cho ImageField (avatar)
Pillow>=11.0

# Render field form kèm class Bootstrap trong template gọn gàng
django-widget-tweaks>=1.5

# Đọc biến môi trường từ file .env
python-dotenv>=1.0

# --- Dependencies for Production (Render/Cloud) ---
gunicorn>=21.2.0
whitenoise>=6.6.0
dj-database-url>=2.1.0
